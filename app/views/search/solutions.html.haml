:javascript
  function convertToIframe(event) {
    event.preventDefault();
    var div = event.target.closest('.group');

    var iframe = document.createElement('iframe');
    iframe.setAttribute(
      'src',
      'https://www.youtube.com/embed/' + div.dataset.id + '?autoplay=1&rel=0'
    );
    iframe.setAttribute('frameborder', '0');
    iframe.setAttribute('allowfullscreen', '1');
    iframe.setAttribute(
      'allow',
      'accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture'
    );
    iframe.setAttribute('class', 'h-full w-full cursor-pointer');
    div.parentNode.replaceChild(iframe, div);
  }


%div{class: "flex justify-center pb-10"}
%div{class: " w-full flex flex-col items-center overflow-hidden", "data-controller": 'toggle'}
  %div{class: "w-full px-4 md:px-0 md:w-1/2 flex items-center mt-4"}
    #searchbox_posts{class: "flex-grow"}
    -# = javascript_packs_with_chunks_tag "search/#{params[:action]}", 'data-turbolinks-track': 'reload'
    %div{class: "flex items-cneter p-2 ml-1 cursor-pointer md:hidden", "data-action": 'click->toggle#toggle touch->toggle#toggle'}
      %div{class: "w-4 h-4 text-black"}
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4" />
        </svg>
      -# %div{class: ""}
      -#   Filters

  #results{class: "w-full flex justify-center"}
    %div{class: "mt-6 md:mt-10 flex flex-wrap justify-center pb-20 w-full"}
      %div{class: "w-full flex flex-wrap md:flex-nowrap md:items-start"}
        %div{"data-toggle-target": 'toggleable', class: "hidden w-full md:w-min pt-6 pb-10 md:py-0 flex-shrink-0 bg-white md:bg-transparent px-4  md:flex flex-col space-y-10 md:space-y-20"}
          %div
            %div{class: "text-2xl font-semibold"}
              Tags
            #tags{class: "mt-2 w-full"}

          %div
            %div{class: "text-2xl font-semibold"}
              Communities
            #communities{class: "mt-2 w-full"}

        %div{class: "mt-6 md:mt-0 w-full md:px-6 flex flex-wrap px-4"}
          %div{class: "flex items-end w-full"}
            %div{class: "text-2xl font-semibold"}
              -# - if @is_solutions_page
              Results
          -# %div{id: "expand-collapse", class: "text-gray-600 cursor-pointer text-sm hover:underline"}
          -#   Expand
          -# - else
          -#   Requests
          -# %div{class: "ml-2 leading-snug pl-2 border-l border-gray-600 text-gray-600 cursor-pointer text- hover:underline"}
          -#   -# - if @is_solutions_page
          -#   -#   - if @is_top_results
          -#   = link_to 'Requests', requests_path
          %div{id: "hits", class: "mt-6 w-full"}





