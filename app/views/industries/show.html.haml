= render 'layouts/header'
= render 'shared/niches/header', niche: @industry

%div{class: "mt-10 flex justify-center"}
  %div{class: "max-w-screen-lg w-full relative flex"}
    %div{class: "w-full mr-12"}
      - if @industry.posts.today.any?
        %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
          Today
        %div{class: "divide-y divide-gray-300 divide-solid rounded-sm"}
          = render @industry.posts.today
          = render @industry.posts.today
      - if @industry.posts.past_week.any?
        %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
          Past Week
        %div{class: "divide-y divide-gray-300 divide-solid"}
          = render @industry.posts.past_week
      - if @industry.posts.past_month.any?
        %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
          Past Month
        %div{class: "divide-y divide-gray-300 divide-solid"}
          = render @industry.posts.past_week




    %div{class: "w-1/3 flex-shrink-0 flex flex-wrap"}
      %div{class: "w-full flex flex-wrap bg-white rounded-sm"}
        %div{class: "py-3 px-4"}
          %div{class: "w-full text-lg font-medium"}
            About this Industry
          %div{class: "py-1 w-full rounded-lg text-sm "}
            = simple_format @industry.description.gsub(/\n/, '<br/>').html_safe
        -# %div{class: "w-full"}
        -#   = "Notify me weekly about products posted to #{niche.title}"
        -# %div{c: "flex items-center py-2"}
        -#   %input{c: "mt-0 block bg-transparent w-full px-0.5 border-0 border-b border-primary focus:ring-0 focus:border-primary",
        -#               "aria-label": "Email", placeholder: "jane@doe.com", type: "text"}
        -#   %button.flex-shrink-0.ml-4.bg-primary.hover:bg-black.border-primary.hover:border-black.text-sm.border-4.text-white.py-1.px-2.rounded{:type => "button"}
        -#     Notify Me!
