-# = render 'layouts/header'
= render 'shared/niches/header', niche: @niche

%div{class: "mt-10 flex justify-center pb-10"}
  %div{class: "max-w-screen-lg w-full flex"}
    %div{class: "w-full mr-12"}
      - if @niche.posts.any?
        - if @niche.posts.today.any?
          %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
            Today
          %div{class: "divide-y divide-gray-300 divide-solid rounded-sm"}
            = render @niche.posts.today
          %div{class: "mt-6"}
        - if @niche.posts.past_week.any?
          %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
            Past Week
          %div{class: "divide-y divide-gray-300 divide-solid"}
            = render @niche.posts.past_week
          %div{class: "mt-6"}
        - if @niche.posts.past_month.any?
          %div{class: "w-full font-bold text-2xl mb-6 leading-none"}
            Past Month
          %div{class: "divide-y divide-gray-300 divide-solid"}
            = render @niche.posts.past_month
      - else
        %div{class: "w-full font-semibold text-2xl leading-none text-center"}
          No solutions yet 😞
        -# %div{class: "mt-1"}
        -# = link_to "Create a new solution", new_post_path, class: "text-blue-500 hover:underline"





    %div{class: "w-1/3 flex-shrink-0 flex flex-wrap"}
      %div{class: "w-full flex flex-wrap rounded-sm"}
        %div{class: ""}
          %div{class: " py-3 px-4 bg-white border border-gray-300"}
            %div{class: "w-full text-base font-bold text-sm text-secondary"}
              = "About #{niche_type}"
            %div{class: "py-1 w-full rounded-lg text-sm"}
              = simple_format @niche.description.gsub(/\n/, '<br/>').html_safe
