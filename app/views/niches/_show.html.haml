= render 'layouts/header'
-# = render 'layouts/filter'

= render 'niches/header', locals: {niche: niche}

-# %div{class: "flex justify-center w-full"}
%div{class: "mt-10 flex justify-center"}
  %div{class: "max-w-screen-lg w-full relative flex"}
    %div{class: "w-full bg-white mr-12"}
      Main

    %div{class: "w-1/3 flex-shrink-0 flex flex-wrap"}
      %div{class: "w-full flex flex-wrap bg-white rounded-lg"}
        %div{class: "px-4 pt-3 w-full text-lg font-medium"}
          About Niche
        %div{class: "px-4 py-1 w-full rounded-lg"}
          = simple_format niche.description, class: "text-sm"
        -# %div{class: "w-full"}
        -#   = "Notify me weekly about products posted to #{niche.title}"
        -# %div{c: "flex items-center py-2"}
        -#   %input{c: "mt-0 block bg-transparent w-full px-0.5 border-0 border-b border-primary focus:ring-0 focus:border-primary",
        -#               "aria-label": "Email", placeholder: "jane@doe.com", type: "text"}
        -#   %button.flex-shrink-0.ml-4.bg-primary.hover:bg-black.border-primary.hover:border-black.text-sm.border-4.text-white.py-1.px-2.rounded{:type => "button"}
        -#     Notify Me!



-# %div{class: "mt-4 px-4 bg-white border border-gray-800 -my-2 -mx-2 py-2 max-w-md rounded-md w-screen max-w-md",
-#       data: { controller: "toggle" }}
-#   %div{class: "text-gray-900 font-semibold text-2xl"}
-#     = niche.title
-#   %div{c: "text-gray-800 text-primary tracking-wider"}
-#     = "n/#{niche.slug}"

-#   %div{class: "mt-2 hidden", data: { target: "toggle.toggleable" }}
-#     %div{class: "mt-2 text-gray-800 border-b border-gray-300 pb-2"}
-#       = simple_format niche.description

-#     %p{class: "text-gray-600 mt-2"}
-#       = "Notify me weekly about products posted to n/#{niche.slug}"

    -# %div{c: "flex items-center py-2"}
    -#   %input{c: "mt-0 block bg-transparent w-full px-0.5 border-0 border-b border-primary focus:ring-0 focus:border-primary",
    -#               "aria-label": "Email", placeholder: "jane@doe.com", type: "text"}
    -#   -# bg-transparent border-none w-full text-gray-700 mr-3 py-1 px-2 leading-tight focus:outline-none",
    -#   %button.flex-shrink-0.ml-4.bg-primary.hover:bg-black.border-primary.hover:border-black.text-sm.border-4.text-white.py-1.px-2.rounded{:type => "button"}
    -#     Notify Me!

-#   %div{class: "flex justify-center"}
-#     %button{c: "pt-1 px-4 rounded-lg transition duration-200 ease-in-out transform",
-#             hover: "scale-150",
-#             f: "outline-none",
-#             data: { action: "click->toggle#toggle touch->toggle#toggle" }}
-#       %div{c: "w-5 h-5 fill-current text-gray-700"}
-#         <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
-#         <path data-target="toggle.toggleable" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
-#         <path data-target="toggle.toggleable" class="hidden" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 15l7-7 7 7" />
-#         </svg>

-# %div{c: "space-y-6 mt-8",
-#     smmd: "space-y-0 grid grid-cols-2 gap-4 mx-4"}
  -# sm: "col-span-2 grid grid-cols-2 pt-4 space-y-0"}
  -# lg: "col-span-3 grid grid-cols-3",
  -# xl: "grid-cols-4"}
  -# - if @products.any?
  -#   = render @products

-# ---- MAIN EMD ----
-# .container
-#   - flash.each do |msg_type, msg|
-#     = content_tag(:div, msg, class: "alert alert-#{msg_type}")
-# = yield
-# = render 'layouts/footer'
-# = debug(params) if Rails.env.development?
