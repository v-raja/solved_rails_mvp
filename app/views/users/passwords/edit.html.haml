-# %h2 Change your password
-# = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f|
-#   = render "users/shared/error_messages", resource: resource
-#   = f.hidden_field :reset_password_token
-#   .field
-#     = f.label :password, "New password"
-#     %br/
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#       %br/
-#     = f.password_field :password, autofocus: true, autocomplete: "new-password"
-#   .field
-#     = f.label :password_confirmation, "Confirm new password"
-#     %br/
-#     = f.password_field :password_confirmation, autocomplete: "new-password"
-#   .actions
-#     = f.submit "Change my password"
-# = render "users/shared/links"

%div{class: "h-screen overflow-hidden"}
  %div{class: "flex flex-wrap justify-center h-full"}
    %div{class: "h-1/5 w-full"}
    -# 2 Column
    %div{class: "max-w-screen-lg w-full h-full relative flex flex-wrap px-6 md:flex md:flex-nowrap"}
      -# Left Column
      %div{class: "w-1/2 mt-16"}
        %div{class: "fill-current h-8 w-48"}
          -# Logo
          = link_to root_path do
            = svg_tag "logo"
        %div{class: "mt-9 text-2xl"}
          Find better solutions to your problems.

      %div{class: "w-1/2"}
        %div{class: "ml-24 bg-white pt-4 pb-6 shadow-lg rounded-lg"}
          = form_for resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "flex flex-col items-center w-full px-4"} do |f|
            = f.hidden_field :reset_password_token
            = f.password_field :password, autofocus: true, autocomplete: "new-password", placeholder: "New Password", class: "w-full py-3  rounded-md border-gray-400 focus:border-indigo-300"
            %div{class: "mt-3"}
            = f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "Confirm New Password", class: "w-full py-3  rounded-md border-gray-400 focus:border-indigo-300"
            %div{class: "mt-3"}
            = f.submit "Change My Password", class: "text-center font-bold text-white py-2.5 w-full bg-primary rounded-md text- hover:bg-opacity-90 cursor-pointer"
            %div{class: "mt-4 w-full justify-center"}
            = link_to "Log In instead", new_session_path(resource_name), class: "text-primary text-sm font-medium hover:underline"
            -# %div{class: "mt-4 border-b w-full border-gray-400"}
            -# %div{class: "mt-6 flex"}
            -#   = link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name), class: "font-medium text-secondary hover:underline"
