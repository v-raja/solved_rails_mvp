= link_to product do
  %div{class: "bg-white p-5 flex flex-wrap"}
    %div{class: "w-full aspect-w-1 aspect-h-1"}
      = image_tag product.thumbnail_url, class: "object-cover"
    %div{class: "mt-4 text-lg font-medium"}
      = product.name
